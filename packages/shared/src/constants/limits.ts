/**
 * Maximum number of points allowed in a single stroke.
 * Prevents memory bloat from extremely long strokes.
 */
export const MAX_STROKE_POINTS = 10000

/**
 * Maximum number of participants allowed in a single meeting room.
 * MVP target scale for team meetings.
 */
export const MAX_PARTICIPANTS = 10

/**
 * Token expiry time in seconds (1 hour).
 */
export const TOKEN_EXPIRY_SECONDS = 3600
